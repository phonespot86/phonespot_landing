# Claude Code로 옮기는 방법

이 폴더 전체가 **GitHub 저장소에 그대로 들어갈 내용**입니다.

## 1. Claude Code 설치

```bash
# macOS / Linux
curl -fsSL https://claude.ai/install.sh | bash

# 또는 Homebrew
brew install --cask claude-code

# 또는 npm (Node.js 18 이상)
npm install -g @anthropic-ai/claude-code
```

Windows는 WinGet, Linux는 apt/dnf/apk로도 설치됩니다.
로그인은 브라우저로 진행되고, 지금 쓰시는 계정 그대로 됩니다.

## 2. 저장소 받아오기

```bash
git clone https://github.com/phonespot86/phonespot_landing.git
cd phonespot_landing
```

## 3. 이 폴더 내용을 저장소에 복사

```
CLAUDE.md          ← 새로 추가 (핵심)
index.html         ← 기존 파일 교체
assets/            ← 새로 추가 (이미지 3장 + 폰트)
docs/              ← 새로 추가 (상세 문서 3건)
worker/            ← 새로 추가 (Cloudflare Worker, 참고용)
examples/          ← 새로 추가 (캠페인 실험 파일)
이관-방법.md       ← 이 파일. 옮기고 나면 지워도 됨
```

## 4. 시작

```bash
claude
```

폴더 안에서 실행하면 **CLAUDE.md를 자동으로 읽습니다.** 지금까지의 규칙·구조·함정·실측 데이터가 전부 들어 있어서, 첫 대화부터 맥락을 알고 시작합니다.

첫 명령으로 이렇게 확인해보세요:

```
CLAUDE.md 읽고 이 프로젝트가 뭔지 요약해줘
```

## 5. 배포가 훨씬 편해집니다

지금은 파일을 받아서 GitHub에 직접 올리셨죠. Claude Code에서는 이렇게 끝납니다:

```
하단 CTA 문구 바꾸고 커밋해서 푸시해줘
```

git 명령을 직접 실행하니까 **다운로드 → 업로드 과정이 사라집니다.** GitHub Pages가 push를 감지해서 자동 배포하고요.

---

## 알아두실 차이점

**되는 것**
- 파일 편집, git 커밋·푸시·PR
- 브라우저 자동화 — `claude --chrome`으로 Claude in Chrome 확장과 연결하면 페이지 테스트, GA4/GTM 관리 화면 조작 모두 가능합니다 (지금 이 세션에서 하던 것과 같은 방식)
- Playwright 같은 테스트 도구는 직접 설치해서 쓰면 됩니다

**안 되는 것 / 달라지는 것**
- **claude.ai 프로젝트 문서는 못 읽습니다.** 그래서 `docs/` 폴더로 옮겨 담았습니다. 앞으로는 프로젝트 문서 대신 이 폴더를 갱신하면 됩니다.
- 이 대화 내용 자체는 넘어가지 않습니다. 다만 결정과 이유는 CLAUDE.md와 docs에 다 정리해뒀습니다.
- 파일을 채팅으로 주고받는 기능은 없습니다. 로컬 파일을 직접 고칩니다.

## 유지 관리 팁

- **CLAUDE.md는 200줄 아래로 유지하세요.** 길어지면 지시를 덜 따릅니다. 상세한 건 `docs/`에 두고 CLAUDE.md에서는 가리키기만 하면 됩니다. (현재 89줄)
- 새로운 결정이나 함정을 발견하면 그때그때 CLAUDE.md나 docs에 적어달라고 하세요. 다음 세션이 그걸 읽고 시작합니다.
- 저장소에 커밋하면 나중에 다른 사람이 합류해도 같은 맥락을 공유합니다.

## 그래도 Cowork가 나은 경우

- GA4·GTM 관리 화면을 길게 만지는 작업 (지금 세션에서 하던 방식이 이미 익숙함)
- 로컬에 개발 환경을 두고 싶지 않을 때
- 폰으로 확인만 하고 싶을 때

둘 중 하나를 고를 필요는 없습니다. 코드 작업은 Claude Code, 분석·설정 작업은 Cowork로 나눠 쓰셔도 됩니다. 저장소가 진실의 원본이 되니까요.
