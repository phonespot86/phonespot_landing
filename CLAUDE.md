# 폰스팟 랜딩페이지

휴대폰 성지 "폰스팟"의 스크롤 내러티브 랜딩페이지. 목적은 **씨티몰(citymall.co.kr) 시세조회로 유입**시키는 것.

- 배포: https://phonespot86.github.io/phonespot_landing/ (GitHub Pages, main 브랜치)
- 방문자는 **거의 전부 모바일**. 모바일 기준으로 판단할 것.

## 작업 규칙

1. **`index.html` 단일 파일이 본체다.** 새 버전 파일(`index_v2.html` 등)을 만들지 말고 이 파일을 직접 고친다.
2. 프레임워크·빌드 도구 없음. **외부 JS 라이브러리 0개.** 이 제약을 유지한다.
3. 수정 후에는 **반드시 실기기 폭(390×844)으로 렌더 검증**한다. 스크롤 엔진이 민감해서 눈으로 확인하지 않으면 깨진 걸 놓친다.
4. 문자열 치환으로 패치할 때는 **치환 횟수를 assert**한다. 조용히 실패한 이력이 여러 번 있다.
5. 섹션을 새로 추가하면 엔진의 **모든 연동 지점**을 함께 수정해야 한다 (아래 참고).

## 파일 구조

```
index.html              본체 (약 65KB)
assets/                 이미지 3장 + Pretendard 서브셋 폰트 + OFL 라이선스
worker/                 Cloudflare Worker (씨티몰 시세 중계) — 별도 배포
examples/               캠페인 레이어 실험용 (본체 아님)
docs/                   상세 문서 — 아래 요약보다 자세한 내용은 여기
```

`assets/`는 거의 안 바뀐다. index.html만 고쳐서 push하면 배포 끝.
**이미지를 교체할 때는 파일명을 바꿔야** 브라우저 캐시가 갱신된다 (`phone-s26-2.webp`).

## 페이지 구조 (5장면)

| Scene | id | 카피 (대구 구조) |
|---|---|---|
| 1 가격표 | `w1` | 히어로 질문 → "제각각 흩어진 성지 시세표는" → "한눈에 볼 수 있는 폰스팟 가격표로." |
| 2 은어 | `w2` | "복잡한 통신사 조건과 은어는 → 누구나 알기 쉬운 간결한 정보로." |
| 3 조건 | `w25` | "무리하게 끼워 넣던 조건들은 → 딱 필요한 만큼만." |
| 4 최종가격 | `w3` | "물어봐야만 알 수 있던 가격은 → 바로 확인할 수 있는 최종 가격으로." |
| 5 클로징 | `closing` | "휴대폰 구매, 이제 더 이상 어렵지 않습니다." + 로고 + CTA + 사업자정보 |

카피는 전부 **"과거 → 현재" 대구**로 맞춰져 있다. 새 문구를 쓸 때 이 리듬을 깨지 말 것.

## 스크롤 엔진 (가장 조심할 부분)

- **PC**: 네이티브 스크롤 + `position:sticky` 핀 + 속도 제한기(1.15화면/s)
- **모바일**: **가상 스크롤(스크롤 하이재킹)**. `#vport`/`#vbody`를 transform으로 직접 움직이고 하향 속도를 1.7화면/s로 제한. 네이티브 스크롤을 쓰지 않는다.
- 구조: `section.wrap`(긴 높이) > `div.pin`(화면에 고정) — 스크롤 진행률이 곧 애니메이션 진행률
- 완료된 섹션은 래치 후 접어서(100svh) 총 길이를 줄인다
- 화면 근처 장면만 렌더한다 (성능)

**새 장면을 추가하면 고쳐야 할 곳**: `layout()` 배열, `collapseDone` 배열, PC 속도제한 구간 목록, `vApply` 배열, `vPins` 루프, `tick`의 secP/done/follow/near-render, 터치 높이 블록, reduce-motion 블록. 하나라도 빠뜨리면 스크롤이 어긋난다.

## 시세 연동

`window.PRICE_API`(Cloudflare Worker) → 씨티몰 백엔드(qwe123.co.kr)에서 45개 모델 시세를 받아온다.

- S1 가격표 3행 + S4 최종가격 카운트다운이 **실데이터로 채워진다**. API 실패 시 고정값 폴백.
- 캐시 30분. 값을 바꿀 땐 worker의 `cacheKey` 버전(`/v8`)도 같이 올려야 즉시 반영된다.
- ⚠️ **씨티몰은 딥링크가 안 된다** (React Router 내부 state 방식). 모든 링크는 `citymall.co.kr/pb` + UTM으로만 보낸다. 개발자에게 파라미터 지원 문의 중 — 지원되면 worker의 `linkFor()` 한 줄만 고치면 된다.
- Cloudflare 무료 플랜 = 요청 10만/일. **워커 내부 캐시 히트도 요청으로 집계**되므로, 호출 수를 줄이는 건 브라우저에 보내는 `Cache-Control`뿐이다.

## 추적

GTM(`GTM-53PX7FSF`) 경유로 GA4(`G-LESY5TX0XM`) + 메타 픽셀 + 당근에 전송.

- `window.GA_ID`는 **반드시 비워둘 것.** 채우면 GTM과 이중 집계된다.
- 이벤트: `Scene1~5_Enter` / `Scene1~5_click` / `citymall_click` / `kakao_click` / `scroll_depth` / `scene_time`
- 주요 이벤트(전환): `kakao_click`, `citymall_click`
- GTM·GA 작업은 **phonespot86@gmail.com** 계정으로 (`?authuser=3`)

## 알아둘 실측 결과 (GA4, 2026-07-13~08-09)

- 활성 사용자 247명, 방문자의 **약 38%가 씨티몰로 이동** (citymall_click 117건/95명)
- 하단 고정 바가 주력 동선 (bottom_bar 94 : closing 19)
- **`kakao_click`은 실사용자 클릭 사실상 0** — 버그 아님(클릭→GA4 도달 실증 완료).
  "가격 확인하기"가 상담 버튼을 완전히 흡수하는 구조. 상담 니즈는 가격 확인 *이후*에 생긴다.
- 평균 참여 시간 7초 → **섹션을 늘리는 방향은 위험하다.**

## 과거에 실제로 터진 함정

- 투명 오버레이(`.h-center`)가 클릭을 가로채 링크가 죽었다 → 구조만 보지 말고 `elementFromPoint`로 실제 클릭 도달을 확인할 것
- 스크롤 테스트 시 섹션 접힘 때문에 offset이 변한다 → 매 반복마다 좌표를 다시 계산할 것
- 모바일 판정거림: 매 프레임 전체 렌더 + GPU 효과가 원인이었다 → 근처 장면만 렌더, backdrop-filter 제거

## 상세 문서

`docs/` 안에 있다. 작업 전에 관련 문서를 읽을 것.

- `docs/구성요소-정의서.md` — 장면별 연출·링크·핀 길이 수치
- `docs/추적-설정.md` — GTM/GA4/메타/당근 전체 설정값
- `docs/시세연동.md` — 씨티몰 API 구조, 워커, 캐시 정책
